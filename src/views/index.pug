doctype html
html
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1, shrink-to-fit=no')
    title INKA 2021
    // Bootstrap core CSS
    link(rel='stylesheet', href='https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.5.2/css/bootstrap.min.css')
    style.
      /*
      * Globals
      */
      /* Links */
      a,
      a:focus,
      a:hover {
      color: #fff;
      }
      /* Custom default button */
      .btn-secondary,
      .btn-secondary:hover,
      .btn-secondary:focus {
      color: #541824;
      text-shadow: none;
      /* Prevent inheritance from `body` */
      background-color: #fff;
      border: .05rem solid #fff;
      }
      /*
      * Base structure
      */
      html,
      body {
      height: 100%;
      background-color: #541824;
      }
      body {
      display: -ms-flexbox;
      display: flex;
      color: #fff;
      text-shadow: 0 .05rem .1rem rgba(0, 0, 0, .5);
      box-shadow: inset 0 0 5rem rgba(0, 0, 0, .5);
      }
      .cover-container {
      max-width: 42em;
      justify-content: center;
      }
      /*
      * Cover
      */
      .cover {
      padding: 0 1.5rem;
      }
  body.text-center
    .cover-container.d-flex.w-100.h-100.p-3.mx-auto.flex-column
      main.inner.cover(role='main')
        h1.cover-heading INKA 2021
        p.lead
          | Här håller vi på att bygga den nya sidan för I-sektionen på LTHs arbetsmarknadsmässa, 
          | om du letar efter inloggningssidan hittar du den 
          a.btn-link(href='/login') här
          | . 
          br
          br
          |  Om du vill komma i kontakt med årets projektledare kan du skicka ett mail till 
          a(href='mailto:inka@isek.se') inka@isek.se
          |  eller ringa +46 (0)70-293 81 42