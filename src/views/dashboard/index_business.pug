doctype html
html
  include includes/head.pug

  body
    include includes/navbar.pug

    .container.mt-5
      .row
        .col
          .row
            .col
              h1= business.name
            .col-2
              a.btn.btn-primary(role='button', href='/dashboard/edit') Redigera information
          .row(style='margin-top: 24px;')
            .col
              label
                strong Kontaktperson i INKA
              p!= business.inka.name + ' &lt;<a href="mailto:' + business.inka.username + '">' + business.inka.username + '</a>&gt;'
              label
                strong Er kontaktperson
              p= business.contact.name + ' <' + business.contact.email + '>'
          
          .row(style='margin-top: 24px;')
            .col
              h3 Marknadsföringsinformation
          .row
            .col
              label
                strong Om oss
              p= business.info.about
          .row
            .col
              label
                strong Vi erbjuder
              each offer in business.info.offers
                .form-check
                  input.form-check-input(type='checkbox' disabled checked=offer.value)
                  label.form-check-label= offer.name

        //.col-4
          h2 Status
      

    include includes/scripts.pug